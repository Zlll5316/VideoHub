#!/bin/bash
# 查看后端运行日志

cd "$(dirname "$0")"

if [ -f "backend.log" ]; then
    echo "📋 后端运行日志（最后50行）："
    echo "=================================="
    tail -50 backend.log
    echo ""
    echo "=================================="
    echo "💡 提示：使用 'tail -f backend.log' 可以实时查看日志"
else
    echo "❌ 日志文件不存在，后端可能未启动"
fi
